<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background rectangle with rounded edges -->
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="@color/primary"/> <!-- Change to desired background color -->
            <corners android:radius="10dp"/> <!-- This will give the rounded edges -->
        </shape>
    </item>

    <!-- Add more items here if needed (like gradient, etc.) -->

</layer-list>